services:
  broadcast-embed:
    build: .
    container_name: broadcast-embed
    restart: unless-stopped
    environment:
      - PORT=3000
      - TZ=America/Los_Angeles
    networks:
      - proxy

networks:
  proxy:
    external: true
